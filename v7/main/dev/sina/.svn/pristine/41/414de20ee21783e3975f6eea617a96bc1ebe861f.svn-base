/**
 * @id Core.Math.getUniqueId
 * 获取唯一ID
 */
$import("sina/core/math/_math.js");
Core.Math.getUniqueId = function (_start, _end){
	return parseInt(Math.random()*10000).toString()+(new Date).getTime().toString();
};
